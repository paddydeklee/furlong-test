const { openBrowser, evaluate, press, goto, waitFor, screenshot, screencast, toRightOf, button, inputField, textBox, focus, write, into, click, doubleClick, $, closeBrowser } = require('taiko');

step("Clear all fields", async () => {
    fields = [];
    forEach 
})